apiVersion: v1
kind: Service
metadata:
  name: fake-api-service
  labels:
    app: fake-api
spec:
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: fake-api
